# Dist

`FugsMultiTrackAudioEX.bundle.js` is **generated**. Rebuild with:

```bash
node scripts/build-bundle.js
```

Includes Core + Effects + Spatial + Dynamics + Switch + Aliases + Compat.  
Does **not** include Docs or Test.
